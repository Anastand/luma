DATABASE_URL="neon db"
#  CLERK ENV VARIABLES
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Only redirect to onboarding on first sign-up, not on every sign-in
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/onboarding
# Leave this empty or set to /dashboard - onboarding page will handle redirects for existing users
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
